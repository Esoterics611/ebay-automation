[pytest]
markers =
    smoke: Fast critical-path tests — run before every deploy
    regression: Full regression suite — run nightly
    slow: Long-running tests (>30 s) — run in dedicated pipeline
addopts = --alluredir=allure-results -ra
testpaths = tests
