<!--
This prompt was run before the current session and was not captured
verbatim. Replace this file's contents with the original prompt text
from your session transcript or notes. Keep it as the prompt exactly
as sent — no editorial commentary.

Resulting artifacts in this repo (for cross-reference):
  pyproject.toml, README.md (initial), CLAUDE.md, .gitignore, .env.example,
  src/ebay_automation/ package skeleton, tests/ directory skeleton,
  atlas/ placeholder files.
-->
